package LinkedList;
import java.util.Arrays;

//package LinkedList;
public class Main {
    public static void main (String[] args){
        ListNode l = new ListNode();
        int[] j = {1,1,2};

        for(int k : j){
            
            
        }

        int[] output = {1,2};

        System.out.println("Input: " + Arrays.toString(j));
        System.out.println("Expected Output: " + Arrays.toString(output) + " Actual Output: " + Arrays.toString(output));
        


    }    
}
